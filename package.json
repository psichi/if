{
  "name": "iffi",
  "version": "0.0.3",
  "description": "Parse an if expression allowing a limited set of values",
  "main": "if.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/psichi/iffi.git"
  },
  "author": "Rob Halff <rob.halff@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/psichi/iffi/issues"
  },
  "devDependencies": {
    "should": "2.x.x"
  }
}
